# Rebuilt

Store manually rebuilt source here.

This is the only layer that should gradually become a maintainable source tree.

Rules:

- Start JS-first unless a module is stable enough for TypeScript
- Keep each rebuilt file traceable back to `recovered/` and `raw/`
- Do not copy large runtime trees here blindly
